language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-larrygenerator' ]; then cd .. && eval "mv $currentfolder generator-larrygenerator" && cd generator-larrygenerator; fi

